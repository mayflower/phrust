# Performance Acceleration Known Gaps

This catalog tracks the gaps that block the staged acceleration program from
being considered complete. It does not replace `docs/performance-known-gaps.md`
or `docs/performance-cranelift-known-gaps.md`; it coordinates the next
interpreter, bytecode, quickening, inline-cache, runtime-fast-path, release, and
default-off native-tier work.

Generated reports under `target/performance/` are local evidence only and must
not be committed.

| Gap ID | Layer | Current evidence | Risk | Planned follow-up |
| --- | --- | --- | --- | --- |
| ACCEL-GAP-REAL-WORKLOAD-CORPUS | Benchmark corpus | Phase 09.01 expanded `tests/fixtures/performance/framework_smoke/` to 9 fixtures covering router dispatch, Composer/autoload-like lookup, DI lookup, DTO hydration, template output, JSON/API response generation, object property/method loops, packed/mixed array traversal, and attribute/reflection warm paths. `just framework-smoke` is now part of `verify-performance` and writes `docs/performance-framework-corpus.md`. Broader real Composer/framework corpus representativeness remains tracked in `docs/performance-known-gaps.md`. | Remaining risk is over-claiming real-world throughput from deterministic micro-smokes. The acceleration gate now covers PHP-like workload shapes, but not full external frameworks or production traffic. | Phase 09.01 closed the acceleration gate; future corpus scale-up remains a general performance gap. |
| ACCEL-GAP-DENSE-BYTECODE | VM execution format | Phase 09.03 added `crates/php_vm/src/bytecode/` with dense opcode/data structures, an IR-to-dense lowerer for a tiny safe subset, verifier coverage for indexes/terminators/spans/source maps, and unit snapshots. Phase 09.04 added `--exec-format=ir|auto|bytecode`, strict bytecode execution for that tiny verified subset, `auto` fallback to rich IR on unsupported lowering/verifier failures, bytecode counters, and `just bytecode-exec-smoke` A/B coverage. Phase 09.05 now covers scalar binary/unary/compare opcodes, full direct-call shape metadata for named and by-reference compatibility, builtin calls through the existing VM call target path, dense array construction/insert/fetch/assign/append opcodes, and by-value foreach init/next. Strict bytecode smoke now includes scalar, comparison, direct-call, packed-array, and framework-like mixed-array traversal fixtures. Phase 09.06 added default-off dense superinstruction selection for three echo-adjacent producer patterns. Phase 09.08 added unified IR/dense quickening site keys and dense quickening smoke for int arithmetic, string concat, and bool branches. FPE-04 adds unsupported and auto-fallback reason counters, lowered/executed family counters, strict unsupported fixtures for property read/write, method/static call, and include, and auto fallback fixtures for object-heavy property/method/static/include shapes. The default remains `ir`. | Dispatch, decoding, cache locality, first quickening sites, superinstructions, direct calls, builtin dispatch through existing semantics, array dims, and by-value foreach now have strict dense execution coverage. Remaining dense gaps are property fetch/assign, method/static calls, unpacked calls, by-reference foreach, include/eval, exceptions/try/finally, generators/fibers, and broader object-heavy framework fixtures. FPE-04 keeps those families fenced because current rich-IR interpreter paths are not exposed as small reusable helpers that preserve exception/fiber propagation, scope, diagnostics, IC metadata, and include/autoload state. | Continue dense expansion only where existing semantic helpers can be reused and A/B fixtures prove parity; property/method/include families now have explicit strict unsupported and auto-fallback accounting for the next follow-up. |
| ACCEL-GAP-SUPERINSTRUCTIONS | Interpreter dispatch | Phase 09.06 added `--superinstructions=off|on`, a dense-bytecode selection pass, fused `load_const_echo`, `load_local_echo`, and `binary_concat_echo` opcodes, selection/execution counters, `scripts/performance/superinstruction_smoke.sh`, and `just superinstruction-smoke` in `verify-performance`. FPE-05 adds strict dense opcode pair/triple mining through `php-vm dump-bytecode-patterns`, `scripts/performance/superinstruction_patterns.py`, `just superinstruction-patterns`, richer candidate/emitted/executed/skipped/fallback counter maps, and `docs/performance-superinstructions.md`. The default remains `off`. | The measured current corpus confirms echo-adjacent producer pairs are the only safe fused set with current helper boundaries. It does not yet fuse compare/jump terminators, binary arithmetic/store chains, call sequences, property access, array/foreach loops, or deopt-capable speculative patterns. | Future expansion must start from the mined report and add only fusions with source-map/debug accounting, generic unfused equivalents, helper reuse, fallback/deopt counters, and A/B fixtures. |
| ACCEL-GAP-FRAME-REUSE | VM frames/register files | Phase 09.07 added explicit frame reuse eligibility on `Frame`, fresh-frame fallback for closure captures, by-reference params/returns, generator/fiber continuations, class contexts, shared top-level locals, try/finally bodies, and destructor-sensitive object-allocation bodies, plus `frames_allocated`, `frames_reused`, `register_files_allocated`, `register_files_reused`, and `frame_reuse_blocked_by_reason` counters. Focused `cargo test -p php_vm frame` coverage exercises repeated calls, recursion, closure captures, by-reference params, finally, destructors, generators, and fibers. | Current reuse is intentionally conservative and does not yet reuse method frames, closure frames, by-reference frames, try/finally frames, or object-allocation frames. That leaves performance upside but preserves PHP-visible lifetime and control-flow behavior. | Phase 09.07 closed the safe initial gate; future expansion should remove individual blocked reasons only with fixture proof. |
| ACCEL-GAP-CALL-BUILTIN-ICS | Calls and builtins | Phase 09.09 extends function-call IC slots with normalized name, lookup epoch, arity, named-argument sequence, by-reference argument shape, and builtin implementation id/version guards. It adds builtin/user function IC hit/miss counters, per-stub builtin counters, exact stubs for `strlen`, `count`, `is_int`, `is_string`, and `is_array`, and `inline-cache-smoke` fixtures for named arguments, by-reference user calls, dynamic function calls, and stub fallback. FPE-06 adds capped polymorphic entries for dynamic string function calls, megamorphic fallback counters, fast-stub fallback reasons, and matrix/smoke fixtures for the dynamic-call shape. | The initial stubs are intentionally tiny and exact; broader builtin stubs, generated arginfo-derived fast stubs, and full per-builtin generic dispatch attribution remain future work to avoid bypassing PHP call resolution, reflection metadata, named-argument errors, by-reference compatibility, coercion, TypeError/ValueError diagnostics, or return-type behavior. | Expand only with differential fixtures for each additional call shape or builtin. |
| ACCEL-GAP-METHOD-CALL-ICS | Method dispatch | FPE-08 adds interpreter method-call IC targets with capped polymorphic receiver entries, receiver class id, class/method epoch, method slot, visibility context, static/final/private/override flags, argument shape, by-reference compatibility metadata, magic `__call` state, direct dispatch counters, and metadata-only tiny-inline candidate/rejection counters. `inline-cache-smoke` includes a method-call guard fixture for final, normal, polymorphic, override, magic, visibility, named-argument, exception, and DTO service loops. | No method body inlining is performed yet. By-reference method parameters are still rejected before executable runtime fixture coverage, and dense-bytecode method dispatch stays fenced behind strict unsupported/auto-fallback accounting. | Future work may add executable by-ref method fallback coverage only after frontend/runtime support lands, then add actual inlining only with semantic fixtures for refs, magic/hooks, exceptions, generators/fibers, named args, `$this` escape, and visibility. |
| ACCEL-GAP-PROPERTY-SHAPE-ICS | Object properties | Phase 09.10 adds explicit property-fetch layout metadata to interpreter IC targets: receiver class id, layout epoch, declared property slot, visibility context, typed-property initialized state, hook/magic flags, and dynamic-property fallback state. FPE-07 adds interpreter-only property-assignment IC metadata for receiver class id, object shape/layout epoch, declared property slot, visibility context, typed-property checks, readonly/init-only state, reference-slot state, hook/magic/dynamic flags, and class/property mutation epoch. `inline-cache-smoke` now includes public read/write, private/protected scope reads and writes, typed valid/invalid writes, dynamic-property fallback, magic `__get`/`__set`, property hooks, DTO hydration loops, and polymorphic/wrong-class receiver fallback; VM unit coverage separately asserts the readonly error fallback counter. `perf-flag-matrix` includes the property-shape and property-assignment fixtures. | Dense-bytecode property fetch/assignment remains intentionally fenced until reusable helpers preserve visibility, magic/hooks, typed/readonly checks, reference state, dynamic properties, diagnostics, and IC metadata. Object-property reference assignment fallback has an internal counter path, but PHP fixtures cannot reach it yet because property references are rejected before runtime lowering. | Safe interpreter fetch and assignment IC gates are closed; dense-bytecode property paths and executable property-reference fallback coverage remain future work. |
| ACCEL-GAP-PACKED-ARRAY-FASTPATHS | Arrays and foreach | Phase 09.11 adds explicit runtime packed metadata (`PackedList`/`MixedHash`, element summary, key-kind summary, numeric-string key ambiguity, reference presence, COW sharing, mutation epoch, packed length), guarded interpreter packed fetch/append/by-value foreach fast paths, family/fallback counter maps, runtime metadata tests, VM fallback tests, and smoke/report counter coverage. FPE-09 adds a guarded packed-int reduction helper, numeric-string fallback attribution, an array-fast-paths-v2 smoke fixture for packed/mixed/overflow/empty/key/reference/COW/mutation/nested/DTO shapes, and Cranelift packed-reduction eligibility that consumes runtime metadata instead of duplicating layout assumptions. Dense bytecode executes the packed-array performance fixture and framework-like packed/mixed traversal fixture through dense array construction, dim fetch/append, builtin `count`/`strtoupper` dispatch, and by-value foreach, with IR-vs-bytecode A/B coverage in `bytecode-exec-smoke`. | Remaining risk is over-expanding array fast paths into by-reference foreach, mutation-during-foreach, COW writes, mixed/record-like arrays, or builtin `array_sum`/`min`/`max` reductions without differential fixtures. Current dense/interpreter gates keep those shapes on generic paths or unsupported fallback, and numeric-string-key ambiguity is attributed as a fallback reason. | Phase 09.11 safe initial gate and FPE-09 metadata/counter/reduction-helper gate closed; full builtin reductions and by-reference/mutation-heavy dense array paths remain future work. |
| ACCEL-GAP-OUTPUT-BUFFER-FASTPATH | Strings and output | Phase 09.12 adds `output_fast_appends` and `output_slow_appends_by_reason`, exact string/true/int echo fast appends, stable generic fallback reasons, output/report docs, and a scalar/nested-buffer smoke fixture. FPE-10 adds safe adjacent IR output batching for exact `Echo` and `LoadConst`/`Echo` producer pairs, `output_batched_appends`, `output_batch_bytes`, concat reserve/preallocation counters, and an output-batching smoke fixture. Phase 09.08 already covers exact string/string concat quickening for IR and dense bytecode with checked preallocation and guard counters. | Remaining risk is future over-expansion into executable output callbacks, broad producer batching, object/resource/array conversion, references, diagnostics, or encoding-sensitive behavior without fixture proof. | Phase 09.12 and FPE-10 close the safe initial output/string batching gates; callback execution and broader conversion fast paths remain future work. |
| ACCEL-GAP-OPTIMIZER-PGO-PASSES | Optimizer | FPE-12 extends the verifier-bracketed constant-folding pass with literal compare folding for proven-safe integer, bool, null, and strict scalar cases; every changed fold is verified with rollback on verifier failure. `php-vm compile --json --opt-level` exposes optimizer pass reports, and `optimizer-diff` archives per-fixture optimizer report JSON plus aggregate attempted/applied/skipped stats. | Remaining risk is over-expanding optimizer passes into loose string/numeric-string comparison, floats, arrays, objects, calls, magic methods, locals/references, COW, warning-producing conversions, block-local dead temporary elimination, redundant load elimination, concat pre-sizing metadata, callsite metadata, or array capacity metadata without verifier and A/B proof. | Continue optimizer expansion one semantics-proven candidate at a time; metadata-only concat/array/callsite work and dead temporary elimination need focused fixtures before implementation. |
| ACCEL-GAP-RELEASE-PGO-BOLT | Production measurement | Phase 09.02 added explicit `release` and `profiling` Cargo profiles plus `just release-benchmark-smoke`, `just pgo-benchmark-smoke`, and `just bolt-benchmark-smoke`. The release smoke is part of `verify-performance` and writes JSON/Markdown under `target/performance/release/`; PGO skips unless `PHRUST_RUN_PGO=1` and `llvm-profdata` are available; BOLT skips outside Linux or without `PHRUST_RUN_BOLT=1`, `PHRUST_BOLT_PERF_DATA`, and BOLT tools. | Remaining risk is over-claiming production speed from local host timings or from optional PGO/BOLT reports that depend on host toolchains and training data quality. | Phase 09.02 closed the required release-measurement gate; future PGO/BOLT tuning remains optional evidence. |
| ACCEL-GAP-BASELINE-JIT-RESEARCH | Baseline native tier | Phase 09.15 added `docs/research/baseline-native-tier.md`, comparing copy-and-patch/stencil, partial-evaluation-derived baseline JIT, Cranelift-only tiering, and interpreter-plus-quickening. The recommendation is research-only for now: no new executable or mock baseline backend is added because the existing default-off Cranelift no-exec/reporting path already covers deterministic native-shape experimentation without adding another execution surface. | Baseline native work still needs executable-memory and W^X policy, ABI/helper hashes, deopt and live-state maps, reference/COW identity, foreach state, try/finally and exception state, generators/fibers, diagnostics, and output-order proof before any executable prototype. | Phase 09.15 closed the required research gate; executable baseline-native work is deferred to a future ADR. |

## Entry Format

- `Gap ID`: stable acceleration identifier used by docs, reports, and final
  audits.
- `Layer`: engine area blocked by the gap.
- `Current evidence`: current file, command, or absence proving the gap.
- `Risk`: correctness, safety, measurement, or policy issue.
- `Planned follow-up`: phase expected to close or refine the gap.

## Closure Policy

A gap is closed only when the owning phase lands code or docs with all required
off switches, fallback paths, counters, fixtures, reports, and validation
evidence. Wall-clock-only evidence cannot close an acceleration gap.
