//! Private imports shared by VM submodules.
//!
//! This is intentionally scoped to `php_vm::vm` implementation modules. Public
//! callers should use the stable `php_vm::vm` re-exports from `mod.rs`.

pub(super) use super::*;
