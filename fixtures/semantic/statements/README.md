# statements

Purpose: structural statement HIR.

Example rules: declarations, globals/statics, echo, unset, labels, goto,
conditionals, loops, switch, try/catch/finally, throw, break/continue, and
return.

Reference classification: accepted in the current fixture set.

Rust diagnostic IDs: none expected.

Known gaps: statement execution is deferred.
