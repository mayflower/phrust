# traits

Purpose: trait declarations, trait-use lists, and adaptation metadata.

Example rules: trait imports, aliases, `insteadof`, and invalid adaptation
shapes.

Reference classification: accepted for valid trait uses; rejected for
reference-confirmed malformed adaptation shapes.

Rust diagnostic IDs: `E_PHP_TRAIT_ADAPTATION_INVALID_SHAPE`.

Known gaps: trait composition conflict resolution is deferred.
