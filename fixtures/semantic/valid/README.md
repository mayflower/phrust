# valid

Purpose: baseline files accepted by PHP lint and by the Rust semantic frontend.

Example rules: minimal open tag handling, simple statements, and empty semantic
diagnostic output.

Reference classification: accepted.

Rust diagnostic IDs: none expected.

Known gaps: none.
