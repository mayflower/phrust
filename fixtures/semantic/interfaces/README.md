# interfaces

Purpose: interface declaration lowering and class-like metadata.

Example rules: interface names, methods, constants, and class-like scope
boundaries.

Reference classification: accepted.

Rust diagnostic IDs: none expected in the current fixture set.

Known gaps: inherited interface compatibility and runtime lookup are deferred.
