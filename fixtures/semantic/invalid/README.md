# invalid

Purpose: baseline parser or semantic rejects.

Example rules: syntax recovery and invalid source handling.

Reference classification: rejected.

Rust diagnostic IDs: parser diagnostics may appear before semantic analysis.

Known gaps: none.
