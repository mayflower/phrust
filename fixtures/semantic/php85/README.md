# php85

Purpose: focused PHP 8.5 surface fixtures routed through the semantic frontend.

Example rules: pipe expressions, clone-with, first-class callables, closures in
constant-expression contexts, casts in constant-expression contexts, and `new`
in constant-expression contexts.

Reference classification: accepted by the pinned PHP 8.5.7 lint oracle.

Rust diagnostic IDs: none expected for the current accepted fixtures.

Known gaps: runtime execution and value semantics for PHP 8.5 features are
deferred.
