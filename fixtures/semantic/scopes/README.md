# scopes

Purpose: lexical scope tree and per-scope metadata.

Example rules: namespaces, functions, closures, arrow functions, methods,
`global`, and `static` declarations.

Reference classification: accepted.

Rust diagnostic IDs: none expected.

Known gaps: runtime closure capture values and include/eval scope mutations are
deferred.
